# Brand v1 — backup

The original CacheLens mark (amber lens ring + three entry bars), kept before the
v2 redesign. Restore from here if v2 is rejected.

| File | Was used at |
|---|---|
| `activitybar-icon.svg` | `packages/vscode-extension/media/icon.svg` — VS Code Activity Bar (monochrome, required) |
| `logo.svg` | `packages/vscode-extension/media/logo.svg` — full-colour mark |
| `marketplace-icon-128.png` | `packages/vscode-extension/media/icon-128.png` — Marketplace listing |
| `nuget-icon.png` | `packages/dotnet/icon.png` — both NuGet packages |
| `docs-logo.svg` | `docs/images/logo.svg` — repo README hero |
| `site-masthead-inline.svg.txt` | inline `<svg>` in `site/index.html` masthead |

Note: the site masthead copy had drifted — it still carried the index-tick path
(`M12 1.5v2.25`) that was removed from the Activity Bar icon for vertical
alignment, so those two were not identical at the time of backup.
